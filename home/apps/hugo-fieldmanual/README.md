# Althea

FieldManual — Hugo-powered operations handbook with searchable playbooks and checklists.

## Run locally

1. Install Hugo (extended)
2. `hugo server -D`

Search index is generated at `/search-index.json` during build.
